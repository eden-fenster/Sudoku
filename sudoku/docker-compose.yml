version: "3.9"
services:
  web:
    build: .
    image: "edenfenster/sudoku_server"
    ports:
      - "5000:5000"
    volumes:
      - .:/sudoku_server
    container_name: sudokuserver_web_1
    depends_on:
      - redis
  redis:
    image: redis
    container_name: sudokuserver_redis
